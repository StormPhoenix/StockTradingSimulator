# MongoDB Configuration
MONGO_USERNAME=admin
MONGO_PASSWORD=admin123
MONGO_DATABASE=stock_simulator
MONGO_PORT=27017

# Mongo Express Configuration  
MONGO_EXPRESS_PORT=8089

# Application Configuration (for future use)
NODE_ENV=development
APP_PORT=3000

# Database Connection String (for application)
MONGODB_URI=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@localhost:${MONGO_PORT}/${MONGO_DATABASE}?authSource=admin

# Security Notes:
# - Change MONGO_ROOT_PASSWORD to a secure password
# - Keep .env file out of version control
# - Use different credentials for production
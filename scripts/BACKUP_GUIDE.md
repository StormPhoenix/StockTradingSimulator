# Speckit 文件备份指南

## 概述

`backup-speckit.sh` 脚本用于备份 speckit 生成的重要中间文件，防止意外丢失。

## 备份内容

脚本会自动备份以下目录：

- **`.codebuddy/`** - CodeBuddy IDE 生成的命令和配置文件
- **`.specify/`** - Speckit 的模板、脚本和内存文件
- **`specs/`** - 项目规格说明文档和相关文件

## 使用方法

### 1. 创建备份

```bash
# 使用脚本
./scripts/backup-speckit.sh

# 或使用 npm 脚本
npm run backup:speckit
```

**输出示例：**
```
📦 开始创建 Speckit 文件备份...

📋 要备份的目录：
  - .codebuddy/ (9 个文件, 88K)
  - .specify/ (11 个文件, 104K)
  - specs/ (16 个文件, 208K)

📊 备份统计：
  - 总目录数: 3
  - 总大小: 400 KB
  - 备份文件: speckit-20260113_222452.zip

✅ 备份创建成功！
```

### 2. 列出现有备份

```bash
# 使用脚本
./scripts/backup-speckit.sh --list

# 或使用 npm 脚本
npm run backup:list
```

### 3. 验证备份完整性

```bash
./scripts/backup-speckit.sh --verify
```

### 4. 清理旧备份

```bash
# 保留最新5个备份，删除其余的
./scripts/backup-speckit.sh --clean

# 或使用 npm 脚本
npm run backup:clean
```

## 备份文件命名规则

备份文件使用以下命名格式：
```
speckit-YYYYMMDD_HHMMSS.zip
```

**示例：**
- `speckit-20260113_222452.zip` - 2026年1月13日 22:24:52 创建的备份

## 存储位置

所有备份文件保存在项目根目录的 `archives/` 文件夹中：
```
StockTradeSimulator/
├── archives/
│   ├── speckit-20260113_222452.zip
│   ├── speckit-20260113_222512.zip
│   └── speckit-20260113_222549.zip
├── .codebuddy/
├── .specify/
├── specs/
└── ...
```

## 自动化建议

### 1. 定期备份

建议在以下情况下创建备份：

- **开始新功能开发前**
- **完成重要里程碑后**
- **修改重要配置文件前**
- **每日工作结束时**

### 2. 集成到工作流

可以将备份命令集成到你的开发工作流中：

```bash
# 开发前备份
npm run backup:speckit && npm run dev

# 提交前备份
npm run backup:speckit && git add . && git commit -m "..."
```

### 3. 自动清理

定期运行清理命令以节省磁盘空间：

```bash
# 每周清理一次
npm run backup:clean
```

## 恢复备份

如果需要恢复备份文件：

1. **找到需要的备份文件**：
   ```bash
   npm run backup:list
   ```

2. **解压备份文件**：
   ```bash
   cd /path/to/project
   unzip archives/speckit-YYYYMMDD_HHMMSS.zip
   ```

3. **确认恢复的文件**：
   ```bash
   # 检查恢复的目录
   ls -la .codebuddy/ .specify/ specs/
   ```

## 故障排除

### 1. 权限问题

如果遇到权限错误：
```bash
chmod +x scripts/backup-speckit.sh
```

### 2. zip 命令未找到

在某些系统上可能需要安装 zip：
```bash
# macOS
brew install zip

# Ubuntu/Debian
sudo apt-get install zip

# CentOS/RHEL
sudo yum install zip
```

### 3. 磁盘空间不足

如果磁盘空间不足：
```bash
# 清理旧备份
npm run backup:clean

# 或手动删除不需要的备份文件
rm archives/speckit-old-timestamp.zip
```

## 最佳实践

1. **定期备份**：建议每天至少备份一次
2. **验证备份**：定期验证备份文件的完整性
3. **清理管理**：定期清理旧备份，保持合理的文件数量
4. **异地备份**：重要项目建议将备份文件同步到云存储
5. **文档记录**：在重要变更时记录备份的原因和内容

## 注意事项

- 备份文件包含项目的敏感信息，请妥善保管
- `archives/` 目录已添加到 `.gitignore`，不会被 Git 跟踪
- 备份过程会排除 `node_modules`、`.git` 等不必要的文件
- 脚本会自动创建 `archives/` 目录（如果不存在）